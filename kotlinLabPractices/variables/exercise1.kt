import java.time.LocalDateTime // Import the LocalDateTime class

fun main() {
  val currentTime = LocalDateTime.now().toString() // Get current time as String
  println("Current time: $currentTime")
}
